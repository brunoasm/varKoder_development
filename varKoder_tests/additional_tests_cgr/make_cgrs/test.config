%Image::ExifTool::UserDefined = (
    'Image::ExifTool::XMP::Main' => {
        'VarkoderKeywords' => {
            Writable => 'string',
            Avoid => 0,
        },
    },
);

1;  # Important for the config file to return true

